# Environment Variables

# ============================================================================
# Development Environment
# ============================================================================

# Vite development server (automatically set by Vite)
# VITE_PORT=5173

# Tauri development mode (automatically set by Tauri CLI)
# TAURI_DEV=true

# ============================================================================
# Build Configuration
# ============================================================================

# Build mode (production/development)
# NODE_ENV=production

# Rust build profile (debug/release)
# CARGO_PROFILE=release

# ============================================================================
# Application Configuration
# ============================================================================

# Application identifier (must match tauri.conf.json)
# TAURI_APP_IDENTIFIER=com.thirdscreen.app

# Application version (synced from package.json)
# TAURI_APP_VERSION=1.0.0

# ============================================================================
# CI/CD Configuration
# ============================================================================

# GitHub Actions (automatically set by GitHub)
# CI=true
# GITHUB_ACTIONS=true
# GITHUB_WORKFLOW=<workflow-name>
# GITHUB_RUN_ID=<run-id>
# GITHUB_SHA=<commit-sha>
# GITHUB_REF=<branch-or-tag>

# ============================================================================
# Secrets (NEVER commit these to version control)
# ============================================================================

# Code signing certificate (Windows)
# TAURI_SIGNING_PRIVATE_KEY=<base64-encoded-pfx>
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD=<password>

# Update server configuration (for auto-updates)
# TAURI_UPDATE_SERVER_URL=https://releases.example.com
# TAURI_UPDATE_SERVER_TOKEN=<token>

# ============================================================================
# Optional Features
# ============================================================================

# Enable debug logging in production builds
# RUST_LOG=debug

# Enable Tauri devtools
# TAURI_DEVTOOLS=true

# Disable security warnings (not recommended)
# TAURI_SKIP_DEVSERVER_CHECK=true
