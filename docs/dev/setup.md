# Dev Setup (Draft)

- Node 20+
- npm install
- Rust toolchain + Tauri prerequisites: https://tauri.app/start/prerequisites
- For metrics: ensure platform-specific native deps available (e.g., WMI/PowerShell on Windows; lm-sensors on Linux; iStats/SMC on macOS).
