/**
 * Re-export domain formatters for backward compatibility
 * @deprecated Import from @domain/formatters/system instead
 */
export {
  formatBytes,
  formatPercent,
  formatRelative,
  formatTemperature,
  formatNetworkSpeed,
} from '../domain/formatters/system';
